# ── 模型 (必填二选一: 选预设 + 填 Key; 或者自己写 LLM_BASE_URL) ─────────────
# 预设: deepseek | qwen | siliconflow | openai | openrouter | ollama
PEH_PROVIDER=deepseek
LLM_API_KEY=
# LLM_MODEL=deepseek-chat
# LLM_BASE_URL=https://api.deepseek.com/v1
# 模型额外参数 (JSON), 例如通义关闭思考: {"enable_thinking": false}
# LLM_EXTRA_BODY={}

# ── 联网搜索 (docker compose 自带 SearXNG, 本机直接跑时三选一) ─────────────
# SEARXNG_URL=http://127.0.0.1:8888
# TAVILY_API_KEY=
# BRAVE_API_KEY=

# ── 运行 ────────────────────────────────────────────────────────────────
# 代码执行: on | ask | off (命令行默认 ask, 网页服务默认只在容器里 on)
# PEH_PYTHON=ask
# PEH_MAX_STEPS=30
# PEH_WORKSPACE=./workspace
# PEH_HOME=~/.pocketexpert-harness
# 网页服务对外监听时必须设访问口令
# PEH_ACCESS_TOKEN=
# SEARXNG_SECRET=
